# Dataset Directory

## 📊 Position-Aware Synthetic Datasets

I generated these datasets specifically to test positional encoding methods. The task is to classify sequences based on values at specific positions, which requires the model to understand position.

### Structure

```
data/
├── train/          # 5,000 training samples
├── val/            # 1,000 validation samples  
└── demo/           # 100 demo samples
```

### Format

Each split contains:
- `*_data.csv` - Human-readable (Excel/Pandas)
- `*_data.pkl` - Fast loading (Python/Numpy)
- `dataset_info.json` - Metadata
- `sample_sequences.txt` - Visual samples

### How to Load

```python
import pickle
with open('data/train/train_data.pkl', 'rb') as f:
    data = pickle.load(f)
    
sequences = data['sequences']  # numpy array (5000, 32)
labels = data['labels']        # numpy array (5000,)
```

### Regeneration

If you need more data, run:
```bash
python scripts/generate_datasets.py
```

Generated by Varun Reddy | February 2026
